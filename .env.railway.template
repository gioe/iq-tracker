# Railway Environment Variables Template
# Copy these variables to your Railway dashboard for each service

# =============================================================================
# BACKEND SERVICE (FastAPI) - Required Variables
# =============================================================================

# Application Settings
ENV=production
APP_NAME=AIQ API
APP_VERSION=1.0.0
API_V1_PREFIX=/v1

# Database (Railway will auto-populate this from PostgreSQL service)
DATABASE_URL=${{Postgres.DATABASE_URL}}

# Security - Railway can auto-generate these
SECRET_KEY=<generate-in-railway>
JWT_SECRET_KEY=<generate-in-railway>
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# CORS (Update with your actual domains/iOS app scheme)
CORS_ORIGINS=*

# Rate Limiting
RATE_LIMIT_ENABLED=true
RATE_LIMIT_STRATEGY=token_bucket
RATE_LIMIT_DEFAULT_LIMIT=100
RATE_LIMIT_DEFAULT_WINDOW=60

# Admin Dashboard
ADMIN_ENABLED=true
ADMIN_USERNAME=<set-manually>
ADMIN_PASSWORD=<set-manually>

# Logging
LOG_LEVEL=INFO

# =============================================================================
# CRON SERVICE (Question Generation) - Required Variables
# =============================================================================

# Database (same as backend)
DATABASE_URL=${{Postgres.DATABASE_URL}}

# LLM API Keys (at least one required)
OPENAI_API_KEY=<your-openai-api-key>
ANTHROPIC_API_KEY=<your-anthropic-api-key>
GOOGLE_API_KEY=<your-google-api-key>
XAI_API_KEY=<your-xai-api-key>

# Question Generation Settings
QUESTIONS_PER_RUN=50
MIN_ARBITER_SCORE=0.7
ARBITER_CONFIG_PATH=./config/arbiters.yaml

# Logging
LOG_LEVEL=INFO
LOG_FILE=./logs/generation.log

# Email Alerts (optional)
ENABLE_EMAIL_ALERTS=false
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=<your-email>
SMTP_PASSWORD=<your-app-password>
ALERT_FROM_EMAIL=<from-email>
ALERT_TO_EMAILS=<comma-separated-emails>
ALERT_FILE_PATH=./logs/alerts.json

# =============================================================================
# NOTES:
# =============================================================================
#
# 1. Variables marked with ${{Postgres.DATABASE_URL}} will be auto-populated
#    by Railway when you link the PostgreSQL service
#
# 2. Variables marked <generate-in-railway> can use Railway's "Generate" button
#    to create secure random values
#
# 3. Variables marked <set-manually> must be set by you
#
# 4. For the cron service, at least ONE LLM API key is required
#
# 5. Email alerts are optional - set ENABLE_EMAIL_ALERTS=false if not using
#
