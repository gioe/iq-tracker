# Test iOS

Build and run iOS tests on the simulator.

Run this command:
```bash
cd /Users/mattgioe/aiq/ios && xcodebuild test -scheme AIQ -destination 'platform=iOS Simulator,name=iPhone 15' -quiet
```

Report the test results including any failures.
